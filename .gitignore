# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Playwright cache (browsers live in ~/.cache/ms-playwright by default; ignore any local stash)
.playwright/
playwright-report/

# Scratch renders / measurement output (never committed)
brand/tools/_scratch/

# Build output — outlined distribution masters are reproducible from brand.json
# via `brand/tools/build.sh --write` (kept out of git to avoid stale artifacts)
brand/dist/
